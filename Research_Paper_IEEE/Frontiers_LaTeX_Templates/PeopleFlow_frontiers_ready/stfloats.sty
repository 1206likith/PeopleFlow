\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{stfloats}[2026/04/08 Local compatibility shim]

% The review build in this workspace is single-column, so the float-tuning
% hooks from stfloats are not required for local validation.

\endinput
