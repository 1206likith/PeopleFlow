# AI Engine

Training and inference utilities for PeopleFlow models.

## Model Registry
Trained model paths are tracked in `modules/ai_engine/data/model_registry.json` via `modules/ai_engine/registry.py`.
The backend uses this registry to load the latest model artifacts.

